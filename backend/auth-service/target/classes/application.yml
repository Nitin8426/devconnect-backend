spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/devconnect
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: auth-service
server:
  port: 8081
