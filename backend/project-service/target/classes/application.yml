# src/main/resources/application.yml
server:
  port: 8087

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devconnect
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
